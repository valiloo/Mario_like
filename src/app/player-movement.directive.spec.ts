import { PlayerMovementDirective } from './player-movement.directive';

describe('PlayerMovementDirective', () => {
  it('should create an instance', () => {
    const directive = new PlayerMovementDirective();
    expect(directive).toBeTruthy();
  });
});
