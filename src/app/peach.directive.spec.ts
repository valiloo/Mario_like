import { PeachDirective } from './peach.directive';

describe('PeachDirective', () => {
  it('should create an instance', () => {
    const directive = new PeachDirective();
    expect(directive).toBeTruthy();
  });
});
